- name: Create hosts file
  template:
    src: "{{ hosts_src }}"
    dest: "{{ hosts_dest }}"
    owner: root
    group: root
    mode: "0644"

- name: Set Proxy
  shell: |
    yast proxy set http={{ proxy }}
    yast proxy set https={{ proxy }}

- name: Register OS
  shell: |
    SUSEConnect -e {{ reg_user }} -r {{ reg_code }} -u https://scc.suse.com
    SUSEConnect -p sle-module-legacy/15/x86_64

- name: Install patterns
  zypper:
    name="{{ inst_patterns }}"
    state=latest
    type=pattern

- name: Install packages
  zypper:
    name="{{ inst_packages }}"
    state=latest

- name: Configure saptune
  shell: |
    saptune daemon start
    saptune solution apply HANA
